echo -nnnnn
(env) ?=0
(export) _=
export a= b= c
export "abc"
export 123
echo "'$HOME'"''"" "$HOME" '' '"$HOME"' "$asdsad"
echo $asdsa '$asdasdas' '|' "|"  -> qualquer coisa que vem depois de uma expação de variable que não existe

**valgrind**
espaço
close fds
expr $? + $?
export 123
Set the $PATH to a multiple directory value (directory1:directory2) and ensure that directories are checked in order from left to right.
< $nãoexiste cat
< file_without_permission <infile cat >out >out

go crazy
export var="echo hello | hi"
echo $var
export var="$HOME | $USER"
 export var="echo hello | hi"
< $var cat
